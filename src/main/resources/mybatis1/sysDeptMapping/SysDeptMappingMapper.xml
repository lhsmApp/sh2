<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SysDeptMappingMapper">

	<!--表名   业务单位映射表N -->
	<sql id="tableName">
		tb_sys_dept_mapping
	</sql>
	
<!-- Name	Code	Comment	Default Value	Data Type	Length	Precision	Primary	Foreign Key	Mandatory
业务类型	TYPE_CODE	"业务类型  1工会经费、教育经费凭证,2党费凭证,3社保互推凭证,4公积金互推凭证,5个缴凭证,6应付劳务费凭证,7企业年金提取凭证,8补充医疗提取凭证,9企业年金发放凭证,10评估调整凭证"	' '	VARCHAR(20)	20		TRUE	FALSE	TRUE
账套编码          BILL_OFF    账套编码	' '	VARCHAR(50)	50		TRUE	FALSE	TRUE
单位编码	DEPT_CODE	单位编码	' '	VARCHAR(50)	50		TRUE	FALSE	TRUE
单位映射编码	MAPPING_CODE	单位映射编码	' '	VARCHAR(50)	50		TRUE	FALSE	TRUE -->

	<!-- 字段 -->
	<sql id="Field">
		TYPE_CODE,
		BILL_OFF,
		DEPT_CODE,
		MAPPING_CODE
	</sql>

	<!-- 字段值 -->
	<sql id="FieldValue">
		#{TYPE_CODE},
		#{BILL_OFF},
		#{DEPT_CODE},
		#{MAPPING_CODE}
	</sql>

	<!-- 新增 -->
	<insert id="save" parameterType="pd">
		insert into
		<include refid="tableName"></include>
		(
		<include refid="Field"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>

	<!-- 删除 -->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		KEY_CODE = #{id}
	</delete>

	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set
		TYPE_CODE = #{TYPE_CODE},
		BILL_OFF = #{BILL_OFF},
		DEPT_CODE = #{DEPT_CODE},
		MAPPING_CODE = #{MAPPING_CODE}
		where
		KEY_CODE = #{KEY_CODE}
	</update>

	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where
		KEY_CODE = #{KEY_CODE}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- Jq分页列表 -->
	<select id="datalistJqPage" parameterType="jqPage" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		<where>
			PARENTID = #{pd.ParentId}
			<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
				and
					(
					
					<!--	根据需求自己加检索条件
						字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
						 or 
						字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
					-->
					)
			</if>
		</where>
				
		<if test="currentResult != null and rowNum != null" >           
              limit #{currentResult}, #{rowNum} 
        </if>
	</select>

	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
	</select>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		KEY_CODE in
		<foreach item="item" index="index" collection="array" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</delete>

	<!-- 张晓柳 -->
	<select id="getNotConfirmMappingList" parameterType="com.fh.entity.SysDeptMapping" resultType="pd">
		select t.MAPPING_CODE, ifnull(d.NAME, ' ') MAPPING_NAME
		from <include refid="tableName"></include> t
		left join oa_department d on t.MAPPING_CODE = d.DEPARTMENT_CODE
		where t.TYPE_CODE = #{TYPE_CODE}
		and t.BILL_OFF = #{BILL_OFF}
		and t.DEPT_CODE = #{DEPT_CODE}
		and t.MAPPING_CODE not in (select RPT_DEPT from tb_sys_confirm_info where STATE = '1')
		order by t.MAPPING_CODE
	</select>
</mapper>